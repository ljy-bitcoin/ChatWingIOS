兼容补丁2（需先安装第一版源码补丁）

本次仅替换 Shared/SharedStore.swift。
解压后将 Shared 文件夹上传到 GitHub 仓库根目录，提交后用 Run workflow 运行 main，不能 Re-run 旧任务。
下载新任务产物、解压、改名 ChatWing-fix2.ipa，再用爱思重新签名安装。

修改：不再先向默认通配符钥匙串组写探测项。读取主程序或所属 App 的描述文件中的具体 application-identifier 候选，并由系统钥匙串 API 实际验证写入和读取权限。描述文件本身不被视为授权，系统拒绝时仍停止。
适用本次上传包：主程序与两个扩展包含相同 application-identifier。其他签名器可能生成不同标识，不能据此保证扩展共享。
保留 App Groups 正常路径；保存后须继续测试录屏扩展是否能读到配置、画中画是否更新及键盘是否能读结果。
本次已通过工程结构与 Swift 语法检查；无本地 Xcode，未完成 SDK 编译或真机验证。
若仍失败，新错误带“兼容版2”及写入/读取状态码，请提供完整截图。不要反复重新保存。
